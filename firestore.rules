rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All data is accessed through the API, which verifies invitations and publication.
    // Client SDKs must never bypass that authorization boundary.
    match /{document=**} { allow read, write: if false; }
  }
}
